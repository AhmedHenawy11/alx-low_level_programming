#include <stdio.h>
#include "main.h"

/**
* puts_half - print half of string.
* @str:the string
*/

void puts_half(char *str)
{
	int x;

	for (x = 0; a[x] != '\0'; x++)
		x++;

	for (x /= 2; a[x] != '\0'; x++)
	{
		_putchar(a[x]);
	}
	_putchar('\n');

}
