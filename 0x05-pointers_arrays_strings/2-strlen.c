#include <stdio.h>
#include "main.h"
#include <string.h>

/**
* _strlen-return lenth of pointer of characters
* description: return lenth of pointer of characters
* @s: characters pointer
* Return: int of lenth
*/

int _strlen(char *s)
{
	return (strlen(*s));
}
