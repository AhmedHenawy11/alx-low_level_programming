#include <stdio.h>
#include <stdlib.h>

/**
 * main - argc, argv
 * description: return the name of program
 * @argc: argumnts count
 * @argv: arguments passed
 * Return: (0)
*/

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
